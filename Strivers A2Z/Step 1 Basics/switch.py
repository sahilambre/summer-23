
import math
def switchCase(choice, arr):
    if choice == 1:
        var = arr[0]
        return var * math.pi * var
    elif choice == 2:
        var_1 = arr[0]
        var_2 = arr[1]
        return var_1 * var_2
        

# arr = [1,2,3]
# print(len(arr))